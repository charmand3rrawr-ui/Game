node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
packages/client/smoke-output/
assets/previews/
# The loose baked sprites; the archive beside them is what is kept.
assets/procedural/

# Scratch screenshots taken at the repo root while checking a render. These are
# working files, not deliverables, and a blanket `git add -A` will otherwise
# sweep them into a commit — which is how two of them got in.
/*.png
/*.jpg

# Scratch output from test harnesses run inside the repo.
/tmp/
